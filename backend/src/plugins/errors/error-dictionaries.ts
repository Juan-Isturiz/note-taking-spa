import { authErrors } from './dictionaries/auth';
import { defaultErrors } from './dictionaries/default';
import { noteErrors } from './dictionaries/note';

export const errorsDictionary = {
  auth: authErrors,
  note: noteErrors,
  default: defaultErrors,
};
